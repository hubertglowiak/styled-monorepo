{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.9.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start-layout": "yarn --cwd packages/styled-layout start",
    "start-app": "yarn --cwd packages/styled-app start"
  }
}
